export function Footer() {
  return (
    <footer className="mt-16 border-t border-black/10 dark:border-white/10">
      <div className="mx-auto flex max-w-6xl flex-col gap-1 px-6 py-6 text-xs opacity-70 sm:flex-row sm:justify-between">
        <span>Middle-earth modded server &middot; Phase 1 foundation</span>
        <span>Canon is a starting point, not a cage.</span>
      </div>
    </footer>
  );
}
