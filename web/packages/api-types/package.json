{
  "name": "@modspec/api-types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types for the mod's HTTP API. Both the website and the mod (via codegen or hand-mirroring) consume this package.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint configured'",
    "test": "echo 'no tests configured'"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
